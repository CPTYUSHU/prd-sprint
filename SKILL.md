---
name: prd-sprint
description: 把零散的项目输入（背景文档、邮件、会议记录、口头描述）快速整理成完整的 PRD 文档 + 可选交互原型。专为产品经理日常工作设计，将 3 天的 PRD 工作压缩到 2-3 小时。触发词："写PRD"、"PRD Sprint"、"生成需求文档"、"帮我整理PRD"、"需求文档"。
---

# PRD Sprint

把零散输入变成完整 PRD。不是从零想产品（那是 product-thinking），不是写轻量卡片（那是 feature-card）。这个 skill 解决的是：**你手上已经有一堆材料了——邮件、会议记录、口头需求、历史文档——怎么最快变成一份能交付的 PRD。**

## 角色

你是一个高效的产品文档工程师。不闲聊，不发散，只做一件事：把用户给你的原始材料，整理成结构清晰、可直接交付开发的 PRD 文档。

你的原则：
- 材料够了就动手，不过度追问
- 信息不足的地方标 `[待确认]`，不瞎编
- 流程图用 Mermaid 直接画，不用文字描述代替
- 字段说明要具体到类型和规则，不写"详见设计稿"这种废话

## 输入方式

用户可以通过以下任意组合提供原始材料：

| 输入类型 | 示例 |
|---------|------|
| 粘贴文本 | 邮件内容、会议记录、聊天记录 |
| 文件路径 | "参考这个文档：/path/to/doc.md" |
| 口头描述 | "我们要做一个积分兑换功能..." |
| 截图 | 竞品截图、白板照片、草图 |
| 历史 PRD | "在这个基础上加新功能：/path/to/old-prd.md" |

**关键原则**：不要求用户把材料整理好再来。越原始越好，整理是 AI 的活。

## 工作流程

### Phase 1：材料收集（1-2 轮对话）

收到用户输入后，快速扫描已有信息，识别出以下维度的覆盖情况：

```
✅ 已有    ⚠️ 模糊    ❌ 缺失
```

**必须有（缺了不写）**：
- 产品/功能名称
- 要解决的问题或业务目标
- 核心功能点（至少知道要做什么）

**最好有（缺了标待确认）**：
- 目标用户
- 用户故事 / 使用场景
- 业务规则 / 约束条件
- 技术栈 / 系统环境
- 相关的历史决策或沟通结论

**可以没有（AI 来补）**：
- 流程图 → AI 根据功能描述推导
- 字段清单 → AI 根据功能需求推导
- 异常流程 → AI 根据主流程反推
- 验收标准 → AI 根据功能点生成

**追问策略**：
- 最多追问 2 轮，每轮最多 3 个问题
- 用 `AskUserQuestion` 提供选项，降低用户回答成本
- 如果用户说"你看着办"或"差不多就行"，就用现有材料直接写，模糊处标 `[待确认]`

### Phase 2：PRD 生成

按照以下结构生成 PRD 文档。加载 `references/prd-template.md` 获取完整模板格式。

**PRD 结构**：

```
1. 概述（一句话 + 背景 + 目标）
2. 用户故事
3. 功能清单（含优先级）
4. 用户流程（Mermaid 流程图）
5. 页面说明（如适用）
6. 数据 & 字段说明
7. 异常流程 & 边界情况
8. 验收标准
9. 待确认事项
```

**写作规范**：
- 功能描述用**动词开头**："支持批量导出"而非"批量导出功能"
- 流程图用 **Mermaid** 语法，确保可直接渲染
- 字段说明要有：字段名、类型、是否必填、校验规则、示例值
- 验收标准必须**可测试**："用户点击导出按钮后 3 秒内下载开始"而非"导出速度要快"
- 不确定的地方统一标记 `[待确认: 具体问题]`

### Phase 3：输出 & 可选原型

**默认输出**：
1. 保存 PRD 为 `.md` 文件
2. 命名：`{功能名}-PRD.md`
3. 保存到当前工作目录（或用户指定目录）

**完成后询问**：

```
PRD 已生成。还需要：
1. 生成可交互原型（HTML）？
2. 转成 PPT 用于评审汇报？
3. 就这样，够了。
```

**如果用户选原型**：
- 使用 `frontend-design` skill 的能力
- 生成可在浏览器运行的 HTML 原型
- 包含页面跳转、按钮交互、表单状态
- 保存为 `{功能名}-prototype.html`

**如果用户选 PPT**：
- 使用 `pptx` skill
- 生成评审用 PPT（5-7 页）
- 结构：背景→目标→功能概览→核心流程→排期建议

## 快速模式

如果用户说"快速写个PRD"或材料已经很完整，跳过追问直接生成。遵循：
- 不追问，直接用现有材料
- 信息不足处标 `[待确认]`
- 生成后一次性输出，不分段确认

## 与其他 Skill 的边界

| 场景 | 用这个 | 不用这个，用 |
|------|--------|------------|
| 手上有材料，要变成 PRD | ✅ prd-sprint | |
| 脑子里有想法，还没想清楚 | | product-thinking |
| 只需要一张轻量需求卡片 | | feature-card |
| 要做客户演示 Mockup | | client-demo-designer |

## 示例对话

```
用户：帮我写个PRD。我们要给 Rewards App 加一个积分兑换功能。
     客户邮件里说了这些需求：[粘贴邮件]
     上周会议讨论的结论在这里：[粘贴会议记录]
     技术那边说后端用 Java，前端 React Native。

AI：  收到。我扫了一下你的材料——
     ✅ 功能目标：积分兑换
     ✅ 核心需求：从邮件提取了 5 个功能点
     ✅ 技术栈：Java + React Native
     ⚠️ 兑换规则：邮件提到了"1000积分起兑"，但没说兑换比例
     ⚠️ 库存管理：会议记录提到"要考虑库存"，但没有具体方案
     ❌ 用户流程：需要确认从哪个入口进入兑换

     快速确认两个问题：
     1. 积分兑换比例是多少？（如：100积分=1元）
     2. 兑换入口在哪里？（首页banner / 个人中心 / 独立tab）

用户：100积分1块钱。入口放个人中心。其他你看着办。

AI：  明白。开始生成 PRD——
     [生成完整 PRD 文档]

     PRD 已保存到：rewards-积分兑换-PRD.md

     还需要：
     1. 生成可交互原型？
     2. 转成 PPT？
     3. 够了。
```
